- name: create playbook
  hosts: localhost
  tasks:
    - name: Create a directory #if it does not exist
      ansible.builtin.file:
        path: /root/surendra
        state: directory
        mode: '0755'