#- name: Demo on loops
#  hosts: localhost
#  tasks:
#    - name: Print debug
#      ansible.builtin.debug:
#        msg: "Hello world"

#
#- name: Demo on loops
#  hosts: localhost
#  tasks:
#    - name: Print debug
#      ansible.builtin.debug:
#        msg: "Hello world"
#      loop:
#        - banana
#        - apple
#        - orange

- name: Demo on loops
  hosts: localhost
  tasks:
    - name: Print debug
      ansible.builtin.debug:
        msg: Fruit name = {{item}}
      loop:
        - banana
        - apple
        - orange
